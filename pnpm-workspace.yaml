packages:
  - "apps/*"
  - "packages/*"

# Allowlist for packages permitted to run lifecycle scripts during install.
# Per-package onlyBuiltDependencies entries must be consistent with this list.
allowBuilds:
  - "@nestjs/core"
  - "@swc/core"
  - "esbuild"
  - "msgpackr-extract"
  - "sharp"
  - "unrs-resolver"
